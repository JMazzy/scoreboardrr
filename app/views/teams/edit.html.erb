<h2>Edit a Team</h2>

<%= form_for @team, url: {action: "update"}, html: {class: "ui form"} do |f| %>
  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <%= f.submit "Submit", class: "ui button" %>
<% end %>
